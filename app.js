// Where x is number of rows and y is number of seats. This theatre has 26 rows and 100 seats per row. Objective is to print out pairs of row number & seatnumber
for (var x = 0; x < 26; x++) {
    for (var y = 0; y < 100; y++) {
        console.log(x, y);
    }
}
